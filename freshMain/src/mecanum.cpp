#include "mecanum.h"

void readPath(std::string yaml_path){
    // YAML::Node pathConfig = YAML::LoadFile(yaml_path);

    // double x,y,theta;
    // for(auto twist : pathConfig){
    //     x = twist["xyz"][0].as<double>();
    //     y = twist["xyz"][1].as<double>();
    //     theta = twist["xyz"][2].as<double>();

    //     MECANUM::moveTo(x,y,z);
    // }
}